diff --git "a/C:\\Temp\\ol-patched\\pristine\\pkg\\src\\core\\token-store.js" "b/C:\\Temp\\ol-patched\\patched\\src\\core\\token-store.js"
index e23ba58..931ee0b 100644
--- "a/C:\\Temp\\ol-patched\\pristine\\pkg\\src\\core\\token-store.js"
+++ "b/C:\\Temp\\ol-patched\\patched\\src\\core\\token-store.js"
@@ -33,8 +33,10 @@ const node_crypto_1 = require("node:crypto");
 const lark_logger_1 = require("./lark-logger.js");
 const log = (0, lark_logger_1.larkLogger)('core/token-store');
 // Dynamic require to avoid security scanner false positive (child-process).
-// CJS (tsc output) has __filename; ESM (tsdown output) has import.meta.url.
-const _require = (0, node_module_1.createRequire)(typeof __filename !== 'undefined' ? __filename : import.meta.url);
+// CJS (tsc output) always has __filename. Never reference import.meta in this file:
+// its mere presence makes Node >=22 syntax detection classify the file as ESM,
+// and require() then returns an empty namespace / fails to resolve relative modules.
+const _require = (0, node_module_1.createRequire)(__filename);
 const _cpMod = ['child', 'process'].join('_');
 const _cp = _require(`node:${_cpMod}`);
 const execFile = (0, node_util_1.promisify)(_cp.execFile);
